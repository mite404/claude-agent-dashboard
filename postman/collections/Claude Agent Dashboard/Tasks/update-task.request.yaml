$kind: http-request
name: Update Task
method: PATCH
url: "{{baseUrl}}/tasks/{{taskId}}"
order: 6000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "running",
      "progressPercentage": 80,
      "startedAt": "{{$isoTimestamp}}"
    }
