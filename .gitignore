# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs (keep the directory, ignore contents)
# Unanchored so nested log dirs (plans/logs/, etc.) are covered too.
# Hook logs contain verbatim user prompts and absolute paths - never commit them.
*.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# database
data/dashboard.db
data/dashboard.db-shm
data/dashboard.db-wal

# caches
.eslintcache
.cache
*.tsbuildinfo
.rumdl_cache

# IntelliJ based IDEs
.idea

# Postman
.postman

# Finder (MacOS) folder config
.DS_Store

# PR watcher state (one file per PR number)
.pr-watcher-*.json

# air MCP config (contains API keys)
.air/
